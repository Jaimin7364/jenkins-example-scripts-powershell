pipeline {
    stages {
        stage('init') {
            steps {
                script {
                    env.myVariable = 'some value'
                }
            }
        }
        // Other stages...
    }
}
